# molding-optima 架构文档

> 本文档记录 molding-optima 融合重写阶段的架构决策与系统设计。
> 当前为**第一部分：项目要解决的问题**。
> 后续将按以下维度逐步补充：
> - 第二部分：应用架构（系统分几层、模块怎么切、接口怎么定）
> - 第三部分：数据架构（领域模型、状态流转、持久化策略）
> - 第四部分：演进架构（迁移路径、阶段规划、验证机制）

---

## 一、项目要解决的问题

### 1.1 项目使命

molding-optima 是一个面向**注射成型工艺领域**的智能工艺优化系统。其核心使命可概括为两件事：

> **1. 辅助注射成型工艺人员设置工艺参数**
>
> **2. 在产品出现缺陷后，根据规则自动调整工艺参数，消除产品缺陷**

前者解决"从零开始如何设参数"的入口问题，后者解决"出现缺陷后如何调参数"的迭代问题。两者覆盖了工艺优化从初始化到缺陷闭环的完整路径。

---

### 1.2 目标用户

| 角色 | 场景 | 核心诉求 |
|---|---|---|
| **注塑工艺工程师** | 新模具/新材料试产、参数调试 | 快速得到一组可用的初始参数 |
| **车间技术员** | 生产现场按工程师指令调机、响应缺陷 | 缺陷出现时获得明确的调整指引 |
| **质量管理人员** | 关注缺陷率与质量数据 | 工艺调整可追溯、可审计 |

**核心用户**：注塑工艺工程师。系统围绕他们的日常工作流设计。

---

### 1.3 核心痛点

当前行业工艺参数设置主要依赖个人经验，存在以下结构性问题：

| 痛点 | 表现 | 后果 |
|---|---|---|
| **经验依赖强** | 工艺参数的合理范围高度依赖老师傅的经验判断 | 新人培养周期长（通常 2~3 年），人员流动即知识流失 |
| **试错成本高** | 不合适的参数直接导致产品报废、模具损耗 | 一次试错可能损失数千至数十万元 |
| **缺陷排查慢** | 出现缺陷后靠经验排查"是什么参数导致的" | 错失生产窗口期，缺陷批量流出 |
| **知识沉淀难** | 经验存在于人脑中，无法文档化、版本化、可审计 | 团队规模扩张时质量参差不齐 |
| **响应滞后** | 缺陷出现到参数调整依赖人工传递 | 反馈链路过长，影响 OEE（设备综合效率） |

---

### 1.4 解决方案

围绕上述痛点，molding-optima 提供**两大核心能力**：

#### 能力一：智能参数初始化

**输入**：模具信息、材料信息、设备信息、工艺设置
**处理**：基于规则库（专家规则 + 案例学习）推算初始工艺参数
**输出**：可解释的初始参数集 + 命中规则的依据

价值：
- 将工艺师从"凭经验试"解放到"基于规则调"
- 初始化时间从"天级"压缩到"分钟级"
- 每个推荐参数都附有依据（哪条规则、哪个系数），工艺师可控、可改

#### 能力二：缺陷驱动的自动调优

**输入**：当前工艺参数 + 缺陷类型/现象
**处理**：根据缺陷规则库匹配调整方案，调整相关工艺参数
**输出**：调整后的参数建议 + 调整依据（关联缺陷、调整幅度、风险提示）

价值：
- 缺陷出现后，从"排查 → 调参"压缩到"标记 → 看到建议"
- 调整过程可追溯：哪条规则被触发、为什么调、调多少
- 案例可沉淀：成功的调优可入库成为新规则

---

### 1.5 价值主张

| 维度 | 价值 |
|---|---|
| **效率** | 工艺初始化从天级降到分钟级；缺陷调优从小时级降到分钟级 |
| **质量** | 调整过程标准化、可审计，不依赖个人经验 |
| **知识沉淀** | 规则与案例可复用、可迭代，人员流动不带走知识 |
| **可解释** | 每个推荐都有规则依据，工艺师保有最终决策权（人机协同而非替代） |
| **可演进** | 规则库与算法可独立升级，系统能力随使用持续增强 |

---

### 1.6 非目标（明确不做什么）

为避免范围蔓延，系统**明确不包含**以下能力：

- **不替代工艺师最终决策**：系统提供建议，决策权在人
- **不做实时闭环控制**：不直接控制注塑机参数（与注塑机控制系统的集成不在本期）
- **不做生产排程、设备监控**：与 molding-expert 业务边界对齐，专注工艺优化本身
- **不做模流仿真、试模管理、成本核算**：与 molding-expert 业务边界对齐

> 这些"非目标"应当作为后续架构决策的边界参考。任何功能扩展都应先回答"这与本系统的核心使命是否一致"。

---

## 二、业务能力

业务能力（Business Capability）是企业"具备什么能力"，与具体实现解耦，是稳定的、抽象的。
按业务价值分三层：核心能力（差异化竞争力）、运营能力（业务运转必需）、支持能力（通用支撑）。

### 2.1 业务能力清单

| 层级 | 能力 | 描述 |
|---|---|---|
| **核心能力** | 生成初始工艺参数 | 基于模具/材料/设备信息，自动推算初始工艺参数 |
| **核心能力** | 推理修正工艺参数 | 根据缺陷/调模记录，匹配规则自动调整工艺参数 |
| **核心能力** | 记录调模过程 | 完整记录调模的操作、结果、缺陷等全过程数据 |
| **核心能力** | 管理调机规则 | 维护专家规则、缺陷规则、调优规则，支持版本化 |
| **核心能力** | 自动生成规则 | 从历史调模记录挖掘新规则，反哺规则库 |
| **运营能力** | 管理模具 | 模具基本信息、产品参数、浇口系统、冷却系统等 |
| **运营能力** | 管理材料 | 材料库、填充物库、推荐参数 |
| **运营能力** | 管理设备 | 注塑机参数、注射单元、辅助设备 |
| **运营能力** | 管理工艺参数 | 工艺参数快照、版本树、调优历史 |
| **支持能力** | 管理用户 | 账号、角色、操作审计 |
| **支持能力** | 管理组织 | 多租户、公司、组织树 |
| **支持能力** | 管理文件 | 模具图纸、材料证书、工艺附件 |
| **支持能力** | 报表与导出 | 工艺参数导出、工艺报告生成 |

### 2.2 能力依赖关系

```
[管理模具/材料/设备] ──→ [生成初始工艺参数] ──→ [记录调模过程]
                              ↓                       ↓
                       [推理修正工艺参数] ─────────────┤
                                                      ↓
                       [管理调机规则] ←──────── [自动生成规则]
```

### 2.3 业务能力 vs 业务流程（架构师核心区分）

**关键原则**：业务流程不等于业务能力。

- **业务能力** = "具备什么能力"（稳定，不易变化）
- **业务流程** = "能力怎么编排"（易变，会随优化而调整）

模块按**能力**划分，不按**流程**划分。流程作为模块设计的参考，但不直接对应模块边界。

> 反例：如果按"调模工作流"作为模块，调模流程优化时整个模块要重构。
>
> 正例：调模工作流是工艺管理模块的**核心场景**，3 个能力（生成初始 / 推理修正 / 调模记录）共享同一套算法和输入输出，合并为"工艺优化引擎"子领域。

---

## 三、功能模块

基于业务能力清单，按架构一致性原则将能力归并到 3 个一级模块。

### 3.1 模块划分原则

- 模块按业务能力划分（不按业务流程）
- 每个模块内部保留清晰的子领域结构（便于未来演进）
- 前后端模块名保持一致（便于接口对齐）
- 共享同一套算法/输入输出的能力应该合并（避免过度拆分）

### 3.2 一级模块

| 模块 | 包含的业务能力 | 子领域 |
|---|---|---|
| **用户管理 (Identity)** | 管理用户、组织、角色/权限 | 用户、组织、角色权限 |
| **基础数据 (Master Data)** | 管理模具、材料、设备 | 模具、材料、设备 |
| **工艺管理 (Process)** | 管理工艺参数、生成初始工艺、推理修正、记录调模过程、管理调机规则、自动生成规则 | 工艺参数、工艺优化引擎、调模记录、调机规则 |

### 3.3 模块结构

```
molding-optima 前端功能模块
├── 用户管理 (Identity)
│   ├── 子领域：用户
│   ├── 子领域：组织
│   └── 子领域：角色权限
│
├── 基础数据 (Master Data)
│   ├── 子领域：模具
│   ├── 子领域：材料
│   └── 子领域：设备
│
└── 工艺管理 (Process)
    ├── 子领域：工艺参数（CRUD）
    ├── 子领域：工艺优化引擎（生成初始 + 推理修正）
    ├── 子领域：调模记录
    └── 子领域：调机规则（含自动生成）
```

### 3.4 工艺管理子领域说明

| 子领域 | 职责 | 演进特点 |
|---|---|---|
| 工艺参数管理 | 工艺参数的 CRUD、版本管理 | 稳定，变更慢 |
| 工艺优化引擎 | 调用算法引擎生成/修正参数 | 算法迭代快，可独立升级 |
| 调模记录 | 调模过程的全链路记录 | 数据沉淀，长期稳定 |
| 调机规则 | 规则 CRUD + 自动生成 | 系统可演进的核心 |

### 3.5 与后端 app 的对应

| 前端模块 | 后端 app | 备注 |
|---|---|---|
| 用户管理 | identity/ | 完全对应 |
| 基础数据 | masterdata/ | 完全对应（含 mold/polymer/injection-machine 子模块） |
| 工艺管理 | process/ | 完全对应（含 parameter/optimization/expert/rule 子模块） |
| 文件附件 | filecenter/ | 不独立前端模块，融入到各业务模块中 |
| 报表导出 | reporting/ | 不独立前端模块，融入工艺管理（如导出工艺参数） |

> 前后端模块可以不一致：后端按技术耦合组织，前端按业务领域组织。

---

## 四、调模工作流（核心场景）

虽然业务流程不等于业务能力，但工艺管理模块的核心场景——**调模工作流**——值得单独说明，因为它是系统价值的核心体现。

### 4.1 工作流定义

```
阶段 1 - 生成初始工艺参数
  输入：模具/材料/设备信息（来自基础数据模块）
  能力：工艺优化引擎 → 生成初始工艺
  输出：工艺参数记录（写入调模记录）

阶段 2 - 推理修正工艺参数
  输入：当前工艺 + 缺陷/试模结果
  能力：工艺优化引擎 → 推理修正
  输出：新的工艺参数（追加到调模记录）

阶段 3 - 记录调模过程
  输入：阶段 1 和 2 的所有操作
  能力：调模记录 → 沉淀
  输出：可追溯的调模历史

数据闭环：调模记录 → 调机规则 → 自动生成规则
```

### 4.2 工作流对前端的要求

- 页面之间的串联要顺畅，不打断业务连续性
- 同一工作流内的页面共享状态（用 Pinia 管理）
- 跨模块数据从基础数据模块带入
- 工作流进度可视化（用户在哪个阶段）

---

## 五、关键技术决策

### 5.1 前端技术栈

| 维度 | 选型 | 理由 |
|---|---|---|
| 框架 | Vue 3 | 与 molding-expert-web 对齐，最终目标 |
| 构建工具 | Vite | 启动快，Vue 3 官方推荐 |
| 语言 | TypeScript | 类型安全，IDE 体验 |
| 状态管理 | Pinia | Vue 3 官方推荐，Vuex 继任者 |
| UI 库 | Element Plus | Element UI 的 Vue 3 版本 |
| 路由 | Vue Router 4 | 与 Vue 3 配套 |
| HTTP | axios | 统一封装、拦截器 |

### 5.2 工程纪律

- 所有模块必须使用上述技术栈，不允许混用
- TypeScript 必须开启类型检查（vue-tsc --noEmit）
- 状态管理按业务模块拆分 Pinia store（identity / masterdata / process）
- API 封装按模块拆分（api/identity.ts、api/masterdata.ts、api/process.ts）
- 任何架构变更（新增模块、调整边界、变更技术选型）必须先更新本文档

### 5.3 已有技术决策（执行记录）

| 日期 | 决策 | 落地产物 |
|---|---|---|
| 2026-09-01 | 前端升级到 Vue 3 + Vite + TypeScript + Pinia + Element Plus | frontend/package.json |
| 2026-09-01 | 删除低维护依赖（vue-print-nb / vue-svgicon / vue-simple-uploader / vuex-module-decorators 等） | frontend/package.json |
| 2026-09-01 | 删除测试框架相关依赖（jest / ts-jest），测试方案后续确定 | frontend/package.json |
| 2026-09-01 | main.ts 重写为 Vue 3 风格（原文件备份为 main.ts.vue2.bak） | frontend/src/main.ts |
| 2026-09-01 | 主题色统一为 #254373（继承老 molding-optima 品牌色） | frontend/src/styles/tokens.scss |
| 2026-09-02 | Element Plus 主题色使用 CSS 变量覆盖（不再用 SCSS 变量覆盖） | frontend/src/styles/element-plus.scss |
| 2026-09-02 | 建立前端样式分层架构（tokens + mixins + element-plus + reset + transition 五层） | frontend/src/styles/ |
| 2026-09-02 | Vite 配置 Sass Modern Compiler API（消除 legacy-js-api 废弃警告） | frontend/vite.config.ts |
| 2026-09-02 | SCSS Mixin 采用模块化函数（`map.get` 代替全局 `map-get`，消除废弃警告） | frontend/src/styles/mixins.scss |

---

## 六、架构演进路线

### 6.1 与上游系统（molding-expert）的关系

molding-optima 是 molding-expert 的**独立子模块**，两者关系如下：

- **数据契约一致**：通过同一份后端 API 契约对齐
- **技术栈独立**：molding-optima（Vue 3）与 molding-expert（Vue 2）各自演进
- **不强制融合**：独立共存是长期状态，不是过渡

### 6.2 关键架构决策记录

#### ADR-001：molding-optima 单方面升级到 Vue 3

| 维度 | 内容 |
|---|---|
| **状态** | 2026-09-01 决策，已生效 |
| **背景** | Vue 2 已于 2023-12-31 停止维护（EOL），生态停滞（Element UI / Vuex 均已迁移到 Vue 3 生态）；技术债务持续累积；molding-optima 是独立子模块，与 molding-expert 共用后端契约，molding-expert 保持 Vue 2 |
| **决策** | molding-optima 升级 Vue 3 + Vite + Pinia + El
ement Plus |
| **取舍** | 不阻塞 molding-optima 演进；接受两套前端共存 |
| **架构纪律** | API 契约文档化、数据结构文档化、视觉规范对齐 |
| **不做的事** | 不主动融合 molding-expert、不共享前端代码、不推动上游升级 |

#### ADR-002：采用渐进式迁移路径，不推倒重来

| 维度 | 内容 |
|---|---|
| **状态** | 2026-09-01 决策，已生效 |
| **背景** | Vue 2 已 EOL（ADR-001）；项目本身简单，部分代码疑似无用；考虑到过推倒重来的高风险与高成本 |
| **决策** | 直接改造当前项目，不新建、不推倒；边迁移边清理死代码 |
| **取舍** | ✅ 业务连续性 + 风险可控 + 业务知识保留 + 团队在迁移中磨砺 Vue 3 能力 |
| **代价** | ⚠️ 短期“半新半旧”窗口；需纪律（边迁移边清理） |
| **不做的事** | ❌ 不新建项目、不推倒重来、不冻结演进 |

#### ADR-003：主题色单一来源化（CSS 变量驱动 Element Plus）

| 维度 | 内容 |
|---|---|
| **状态** | 2026-09-02 决策，已生效 |
| **背景** | Element Plus 默认主题色为 `#409EFF`（蓝），与 molding-optima 品牌色 `#254373`（深酷蓝）不一致；老 molding-expert-web 采用 SCSS 变量覆盖 Element Plus 主题色方案，但该方案有三个问题：1）编译时固定，无法运行时切换；2）需重新构建主题才能变；3）Element Plus 部分场景不读 SCSS 变量 |
| **决策** | 采用 Element Plus 推荐的 CSS 变量覆盖方案：`tokens.scss` 定义 `var(--theme-primary)` → `element-plus.scss` 将 `--el-color-primary` 设为 `var(--theme-primary)` |
| **取舍** | ✅ 运行时可换主题（`setProperty('--theme-primary', ...)`）；✅ Element Plus 所有场景一致响应；✅ 单一来源（只改 tokens.scss 一处即可同步全局）|
| **代价** | ⚠️ 需注意 Element Plus CSS 变量名（如 `--el-color-primary-light-3` 等需手动映射），后期暗色主题需补充 |
| **架构纪律** | 所有颜色走 tokens.scss；不直接在业务代码中出现 `#xxxxxx`；Element Plus 主题不另行维护 |
| **不做的事** | ❌ 不保留 SCSS 变量覆盖方案；❌ 不引入第三方主题包（如 element-theme） |

#### ADR-004：前端样式系统分层架构（五层）

| 维度 | 内容 |
|---|---|
| **状态** | 2026-09-02 决策，已生效 |
| **背景** | 老 molding-expert-web 的 styles 目录混用了 8+ 类样式文件（variables/index/element-variables/mixin/utilities/theme/svgicon/devRefs...），文件职责重叠、导入混乱；未明确哪些是「设计令牌」、哪些是「工具」、哪些是「全局覆盖」 |
| **决策** | 采用五层架构：1）`tokens.scss` 设计令牌（CSS 变量）；2）`mixins.scss` SCSS 工具集（mixin）；3）`element-plus.scss` Element Plus 主题覆盖；4）`reset.scss` CSS Reset；5）`transition.scss` Vue 过渡动画 |
| **取舍** | ✅ 职责单一（令牌 vs 工具 vs 覆盖 vs 重置 vs 过渡）；✅ CSS 变量负责运行时，Mixin 负责编译时；✅ Vite `additionalData` 全局注入 mixin，无需手动 @use |
| **代价** | ⚠️ 业务页面需加 `lang="scss"` 才能使用 mixin（渐进迁移中）；⚠️ mixin 与 var() 职责需明确（混用时可能困惑） |
| **架构纪律** | 新建页面必须 `<style scoped lang="scss">`；令牌查询 tokens.scss，工具查询 mixins.scss；两者都不够才写新代码 |
| **不做的事** | ❌ 不在 tokens.scss 装「组件专属」变量（如 sidebar 色应进 layout 组件内部）；❌ 不混用 SCSS 变量 $ 与 CSS 变量 --（所有运行时常量用 --） |

### 6.3 长期演进原则

- 各自演进、独立部署
- 通过 API 契约保持一致性
- 设计语言对齐（视觉/交互）
- molding-expert 何时升级是它自己的事

---

## 附：文档维护说明

- 本文档由架构师维护，反映当前阶段的架构共识
- 任何架构层面的变更（新增模块、调整边界、变更技术选型）应先更新本文档再实施
- 后续补充第二、三、四部分时，应保持与第一部分的"项目使命"一致

---

*文档版本：v0.5（含 ADR-001、ADR-002、ADR-003、ADR-004）*
*最后更新：2026-09-02*
